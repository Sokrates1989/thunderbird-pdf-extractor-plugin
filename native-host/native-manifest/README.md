# Native host manifest

`installer/windows/install.ps1` generates the machine-specific manifest with an
absolute executable path and registers it for the current user. The fixed host
name is `de.sokrates1989.thunderbird_pdf_archiver`; the only allowed extension
is `thunderbird-pdf-archiver@sokrates1989.de`.
